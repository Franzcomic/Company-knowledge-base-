# 基于 RAG 的企业知识库客服系统——前端专项实施计划

> 本文档为《项目实施计划.md》（`d:\Corpedia\plan\项目实施计划.md`）的前端落地细化方案，说明**用什么框架、怎么搭、按什么顺序做**，并将 8 个功能模块的前端任务对号入座到可直接执行的任务清单，供前端成员（柯荣浩 / 范子灿）排期与并行开发。

---

## 一、技术选型（复用已定选型 + 现成模板）

### 1.1 技术栈
| 用途 | 选型 | 说明 |
|---|---|---|
| 框架 / 构建 | **Vue 3 + Vite + TypeScript** | 已定，Vite 加速开发/构建 |
| 组件库 | **Element Plus** | 已定，直接沿用（与 vue-pure-admin 内建一致） |
| 状态管理 | **Pinia** | 已定，管理登录态 / 用户信息 / 会话上下文 |
| 路由 | **Vue Router** | 已定，配合权限守卫做登录拦截与角色路由 |
| HTTP | **Axios** | 封装拦截器：JWT 注入 + `{code,msg,data}` 解包 + 401 跳登录 |
| 图表（P2） | **ECharts** | 统计看板卡片/趋势/排名/命中率图 |
| 上传 | Element Plus `<el-upload>` | 自带拖拽 + 进度，接文档上传接口 |

### 1.2 模板方案（推荐第三人称成熟，不用从零搭）
| 方案 | 选择 |
|---|---|
| **vue-pure-admin**（Vite + Vue3 + Pinia + Element Plus） | ★ 首选。轻量、与已定技术栈一致、RBAC 动态路由开箱即用、易改成个人/双端结构 |
| vue-vben-admin（新版） | 备选。功能更全但更重，组件库走向 AntD/shadcn，与本项目已定 Element Plus 不一致，改造成本高 |
| 自研骨架 | 仅在需要完全掌控结构时选用，成本最高 |

**推荐结论**：以 **vue-pure-admin** 为基座，删除自带业务示例，保留登录、权限、布局、Axios 封装等能力，按本计划填充本项目页面。

> 提示：知识库级数据权限不在前端做，由后端 `/api/rag/retrieve` 的 Metadata Filter 控制；前端仅做"可见/禁用"维度，避免越权信息暴露。

---

## 二、前端项目结构与模块对应

```
src/
├─ api/                  # 按模块封装接口
│   ├─ auth.ts           # 模块① 登录/登出/me
│   ├─ user.ts           # 模块①(P1) 用户/角色/部门
│   ├─ kb.ts             # 模块② 知识库
│   ├─ document.ts       # 模块② 文档上传/列表/详情/chunks
│   ├─ conversation.ts   # 模块⑥⑦ 会话 CRUD / 历史消息
│   ├─ rag.ts            # 模块④⑤ 检索 / chat（内部契约）
│   └─ stats.ts          # 模块⑧ 统计
├─ stores/               # Pinia
│   └─ user.ts           # 登录态、当前用户与角色、token
├─ router/               # 路由表 + 权限守卫（P0 认证，P1 角色路由）
├─ layout/               # 布局（后台管理框架 + 顶部用户/登出）
├─ views/
│   ├─ auth/             # 登录页（模块① P0）
│   ├─ chat/             # 问答会话主界面（模块⑤⑥⑦，员工端核心）
│   ├─ kb/               # 知识库列表 + 文档管理与上传（模块②）
│   └─ admin/            # 用户/角色/部门管理（P1）· 统计看板（P2 模块⑧）
├─ components/
│   ├─ ChatMessage.vue   # 消息气泡（文本 + 来源引用 + 相似度 + 赞/踩）
│   ├─ SourceCard.vue    # 来源片段卡片（可点击跳转文档预览）
│   └─ ConversationList.vue  # 会话侧边栏（新建/切换/删除）
└─ utils/request.ts      # Axios 拦截器
```

**模块 → 前端页面对应**
| 模块 | 前端页面/组件 |
|---|---|
| ① 用户与权限 | 登录页、个人资料；P1：用户/角色/部门管理页 |
| ② 文档与知识库 | 知识库列表、文档列表、上传（拖拽+进度）、删除确认；P1：文档详情 chunk 预览、重向量化、权限设置 |
| ③ 文档处理管道 | 无独立页面，由上传与文档状态页驱动 |
| ④ RAG 检索 | 无独立页，在问答会话中展示检索片段与相似度 |
| ⑤ 大模型回答 | 问答会话中渲染答案 + 来源引用 |
| ⑥ 智能问答会话 | **问答主界面**：ChatMessage / 输入框 / 多轮 / 会话侧边栏；P1：评价 |
| ⑦ 历史记录 | 会话侧边栏、历史消息查看、续聊 |
| ⑧ 统计与分析 | 管理员统计看板（ECharts），P2 |

---

## 三、实施步骤（按阶段，对号 `项目实施计划.md` 阶段）

### 阶段 1 · 工程搭建与登录（对应计划阶段 1，P0）
- [ ] 基于 vue-pure-admin 初始化工程，安装 Vue3/Vite/TS/Pinia/Router/Element Plus/Axios
- [ ] 配置 `.env`（后端 BaseURL、上传大小限制）、`vite.config` 代理 `/api`
- [ ] 封装 Axios 拦截器：token 注入、`{code,msg,data}` 解包、401 → 登录页
- [ ] 登录页（表单 + 记住密码），调用 `/api/auth/login` 存 token 到 Pinia + 本地
- [ ] 路由守卫：未登录拦截 → 登录页；`/api/auth/me` 拉取用户信息
- 验收：能登录、token 生效、永久会话、退出清除状态。

### 阶段 2 · 知识库与文档管理（对应计划阶段 2，P0）
- [x] 知识库列表页（列表/新建/删除），接 `/api/kb`
- [x] 文档列表页（分类/状态筛选 PARSING/READY/FAILED），接 `/api/kb/{id}/documents`
- [x] 文档上传：「拖拽 + 进度条」，multipart 接 `POST /api/kb/{kbId}/documents`
- [x] 文档删除二次确认，接 `DELETE /api/documents/{id}`
- [x] P1：文档详情 chunk 预览、重新向量化按钮、权限/部门设置表单
- 验收：上传文档后状态流转可见、进度正确、删除生效。

> **阶段2 实施完成记录（2026-09-17，前端）**：
> - 新增 `src/api/kb.ts`：按后端方案 §阶段2/§八 契约封装 知识库（list/create/delete `/api/kb`）、文档（list `GET /api/kb/{kbId}/documents`、上传 `POST /api/kb/{kbId}/documents`（multipart，字段 `file`，带 `onUploadProgress`）、详情 `GET /api/documents/{id}`、删除 `DELETE /api/documents/{id}`）及 P1 扩展（`GET /api/documents/{id}/chunks`、`POST /api/documents/{id}/reprocess`、`PUT /api/documents/{id}/permission`）、部门 `GET /api/departments`；类型对齐契约（`KnowledgeBase`/`DocumentItem`/`DocumentChunk`/`Department`）。
> - 重写 `src/views/kb/KbView.vue`：知识库列表（名称/描述/所属部门/权限级别标签）、新建（名称必填 + 描述 + 所属部门 + 权限级别）、删除二次确认；「文档管理」跳转 `/kb/:id/documents`。
> - 新增 `src/views/kb/KbDocuments.vue`：状态筛选（全部/解析中/已就绪/失败）、拖拽上传（`el-upload` 自定义 `http-request` + 进度条、`.env VITE_UPLOAD_MAX_MB=10` 前置校验）、上传后按 **硬骨头 3 契约轮询 `GET /api/documents/{id}`（每 2.5s、3 分钟超时）** 展示 PARSING→READY/FAILED 状态流转、列表存在解析中文档时每 3s 静默刷新、删除二次确认；P1：详情抽屉（chunk 预览：索引/相似度/内容）、重新向量化（`/documents/{id}/reprocess` + 轮询）、权限/部门设置弹窗（`/documents/{id}/permission`）。
> - 路由注册 `/kb/:id/documents`（`src/router/index.ts`）。
> - 修复阶段1遗留：`@element-plus/icons-vue@^2.3.2` 加入依赖并在 `main.ts` 全局注册图标（消除布局 `ArrowDown` 组件解析警告）。
> - 验证：`npm run build`（vue-tsc 类型检查 + vite build）通过；浏览器冒烟通过（登录 → /kb 渲染与新建弹窗 → /kb/1/documents 渲染，全新加载后 console 无错误）。
> - **待后端阶段2（`KbController`/`DocumentController`/`DepartmentController`）实现后联调**：当前 `/api/kb` 等接口不存在，页面数据请求会失败（属预期）；`/api/departments` 未就绪时新建/权限弹窗的部门下拉为空，不影响核心 P0 流程。

### 阶段 3 · 智能问答主界面（对应计划阶段 3，P0，交互重点）
- [x] **ChatMessage 组件**：消息气泡（用户 / AI），支持多轮连续对话
- [x] **SourceCard 组件**：展示答案来源（document_id/title/chunk/相似度），可点击
- [x] 输入框 + 发送 + 历史消息滚动加载，接 `POST /api/messages`
- [x] 会话侧边栏：新建 / 切换 / 删除，接 `GET/POST/DELETE /api/conversations`
- [x] 历史消息查看与续聊，接 `GET /api/conversations/{id}/messages`
- [x] P1：回答评价（赞/踩 + 原因），接 `POST /api/messages/{id}/feedback`（UI 已实现，接口依赖后端阶段 4，当前优雅降级）
- 验收：用 `知识库测试问题集.json` precision 类问题测通，来源可点击展示。

> **阶段3 实施完成记录（2026-09-17，前端）**：
> - 新增 `src/api/conversation.ts`：按后端方案 §阶段3/§八 契约封装 会话（list/create/delete `/api/conversations`、历史 `GET /api/conversations/{id}/messages`）、提问 `POST /api/messages`（`{conversationId, content}` → `SendMessageResultVO`）、评价 `POST /api/messages/{id}/feedback`（P1）；类型对齐 `Conversation`/`Message`/`Source`/`SendMessageResult`。
> - 新增 `src/components/ChatMessage.vue`：用户/AI 消息气泡、多轮连续对话、生成中占位（"正在检索并生成回答…"）、发送失败占位、拒答标识（answered=false 显示"未在知识库中找到足够可靠的信息（已拒答）"）、来源引用渲染、P1 赞/踩评价（模块级 Map 记忆本次会话评价状态，DOWN 弹原因输入）。
> - 新增 `src/components/SourceCard.vue`：来源卡片（documentId/title/相似度），点击弹「来源预览」弹窗，尝试加载 `GET /api/documents/{id}/chunks` 并按 chunkId（`doc-{id}-{idx}`）定位分块内容；该 P1 接口未实现时静默降级为仅元数据。
> - 新增 `src/components/ConversationList.vue`：会话侧边栏（新建/切换/删除+二次确认、空状态引导）。
> - 重写 `src/views/chat/ChatView.vue`：左侧会话栏 + 右侧消息流/输入区；无会话时首条消息自动建会话（标题由后端首轮自动填充并刷新列表）；发送流程 = 本地即时渲染 USER + 加载占位 → 替换为 ASSISTANT 答案（含来源/answered）；自动滚动到底；Enter 发送 / Shift+Enter 换行；刷新后按会话加载历史续聊。
> - 修改 `src/utils/request.ts`：支持 `{ silent: true }` 请求配置（失败静默不弹错，axios 类型模块扩展），供来源预览/评价降级复用。
> - 修改 `src/api/kb.ts`：`getDocumentChunks` 透传 silent 配置。
> - 验证：`npm run build`（vue-tsc 类型检查 + vite build）通过；浏览器端到端冒烟全通过 —— 登录 → 新建会话 → precision 问题（"员工工作满 1 年，每年可享受几天带薪年假？"）出现加载态→回答+来源卡片（文档 01-01 员工手册.md，相似度 0.59）→ 点击来源弹预览 → non_exist 问题（"公司健身房每周开放时间是几点到几点？"）显示拒答标识 → 会话自动标题 → F5 刷新历史恢复（2 轮续聊）→ 删除会话确认与空状态；console 无业务报错（仅 dev 热重载 ERR_ABORTED）。
> - **遗留（待后端阶段 4 收口）**：① `POST /api/messages/{id}/feedback` 后端未实现（后端阶段4 `FeedbackController`），当前提交评价会 404 并提示"评价功能暂不可用"，前端 UI 就绪；② 来源预览分块内容依赖 `GET /api/documents/{id}/chunks`（后端阶段4），当前降级显示"分块内容暂不可用"；③ 多轮历史进 Prompt 属后端阶段4 收口，前端已支持多轮展示与续聊。

### 阶段 4 · 权限增强（对应计划阶段 4，P1）
- [ ] 用户管理页（列表/启停用/分配部门与角色），接 `/api/users`
- [ ] 角色管理页 + 部门管理页，接 `/api/roles`、`/api/departments`
- [ ] 前端基于角色权限：菜单/按钮级控制（vue-pure-admin RBAC 能力）
- 验收：财务角色仅见财务知识库对应菜单/功能。

### 阶段 5 · 统计看板（对应计划阶段 5，P2）
- [ ] 统计看板布局（数据卡片 + ECharts 图表）
- [ ] 趋势图 / 热门问题排名 / 热门知识库 / 部门分布 / 命中率 / 响应时间 / 满意率
- [ ] 接入 `/api/stats/*` 接口，时间范围筛选
- 验收：统计图与接口数据一致。

---

## 四、组件复用与交付约定

- **问答相关组件（ChatMessage / SourceCard / ConversationList）是本项目核心自研件**，模板无现成替代，建议独立成目录并写最小注释说明数据结构，避免与模板组件耦合。
- 所有列表页复用模板的 ProTable/表格封装；表单复用其表单/弹窗封装。
- 统一返回结构 `{ code, msg, data }` 在 Axios 拦截器中统一处理，页面无需关心 code。
- 样式沿用 Element Plus 主题变量，临时样式不进全局（优先 scoped）。

---

## 五、风险与注意事项

| 风险 | 影响 | 应对 |
|---|---|---|
| AI 聊天组件的来源展示/多轮状态复杂 | 拖慢 P0 阶段 3 | 先做最小可用：输入→回答→来源卡片，多轮与评价放 P1 |
| 大段 LLM 输出卡界面 | 体验差 | 后期用流式（SSE / ReadableStream）分片渲染，接口契约先定 |
| 模板自带示例代码干扰 | 页面混乱 | 阶段 1 即清理自带业务示例，保留权限/布局/Axios 骨架 |
| 外部切换组件库（vben→AntD）导致返工 | 难度大 | 坚持 Element Plus，选中模板前确认组件库一致性 |

---

## 附：与《项目实施计划.md》的对应
- 阶段 1~5 与 `项目实施计划.md` 第七节阶段 1~5 一一对应。
- 前端仅承担 UI 与权限“可见”维度；数据权限、检索质量由后端 / RAG / Milvus 负责，前端不重复实现。
- 前端成员（柯荣浩、范子灿）按“模块为单位”并行领取：先 Chat 主界面，再知识库，最后管理端与统计。