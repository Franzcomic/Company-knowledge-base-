# 前端环境变量
# VITE_API 方向有两种模式：
#   1) 走 vite 代理：值为空时使用 VITE_API_PROXY（默认 localhost:8080），请求路径保持 /api/xxx
#   2) 直连后端：填完整地址，如 http://192.168.1.100:8080
VITE_API_BASE_URL=

# 开发代理目标（后端 Spring Boot）
VITE_API_PROXY=http://localhost:8080

# 单文件上传大小上限（MB），与后端保持一致
VITE_UPLOAD_MAX_MB=10